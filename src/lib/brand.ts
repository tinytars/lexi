// Product identity + Foundation copy now live in the cross-app package @tars/brand
// (packages/brand) so the public site and this dashboard share one source. Re-exported here so
// existing imports (`./brand`) keep working.
export * from "@tars/brand";
